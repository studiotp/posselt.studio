---
export interface Props {
  href: string;
  class?: string;
  target?: string;
  rel?: string;
  title?: string;
}

const { href, class: className, target, rel, title } = Astro.props;
---

<a href={href} class={className} target={target} rel={rel} title={title}>
  <slot />
</a>
